apiVersion: apps/v1
kind: Deployment
metadata:
  name: authenticationservice
  namespace: plandar-development
  labels:
    app: authenticationservice
spec:
  replicas: 3
  selector:
    matchLabels:
      app: authenticationservice
  template:
    metadata:
      labels:
        app: authenticationservice
    spec:
      containers:
      - image: docker.pkg.github.com/ese6-individual-project-floris-feddema/authenticationservice/develop
        imagePullPolicy: IfNotPresent
        name: authenticationservice
        ports: 
        - containerPort: 80
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
      imagePullSecrets:
      - name: github-docker-registry